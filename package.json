{
  "name": "learn-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "rm -rf node_modules/.cache && rm -rf out && yarn build && touch ./out/.nojekyll && gh-pages -t -d out"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "gray-matter": "^4.0.2",
    "next": "9.5.2",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "remark": "^12.0.1",
    "remark-html": "^12.0.0",
    "sass": "^1.26.10"
  }
}
